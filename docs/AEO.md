# Answer engine surface

Define the entity once. Publish canonical Q&A. Attach Organization + FAQ schema. Offer `/llms.txt`. Disallow `/desk` and `/api`.

Independent corroboration moves citations. Repeated self-statements do not.
